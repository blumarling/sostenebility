import React from 'react'
import styled, { css } from "styled-components"

const NullCompo = () => {

  return (
    <NullCompoContainer>
    </NullCompoContainer>
  )

}

const NullCompoContainer = styled.div`
`

export default NullCompo
